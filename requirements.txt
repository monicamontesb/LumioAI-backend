# FastAPI framework
fastapi==0.111.1

# Uvicorn server
uvicorn[standard]==0.23.2

# Pydantic for data validation (FastAPI lo usa internamente)
pydantic==2.7.2

# Optional: SQLAlchemy for future database integration
SQLAlchemy==2.0.24

# Optional: MySQL or PostgreSQL drivers if you add a database later
mysql-connector-python==8.1.0
psycopg2-binary==2.9.7

# Requests library, in case you want to call APIs from backend
requests==2.32.0
